function mostrar()
{

	var contador;
	var positivo;
	var negativo;
	var contadordevueltas;

	contador=0
	positivo=0
	negativo=1

	contadordevueltas=0
	while(contadordevueltas<5)
	{
		
		contadordevueltas++;

	}
	var contadordealumnos;
	contadordealumnos=0
	wile(contadordealumnos<100)
	{
		edad=prompt("ingrese edad");
		edad=parseInt(edad);
		if(edad==66)
		{
			break;
		}
		if(contadordealumnos==100)
		{
			break;
		}
		contadordealumnos=++
	}
	if(contadordealumnos==100)
	{

	}




document.getElementById('suma').value=positivo;
document.getElementById('producto').value=negativo;

}//FIN DE LA FUNCIÓN